services:
  - type: web
    name: openrouter-proxy
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      # OpenRouter no bloquea las IPs de salida de Render (datacenters EE.UU.)
      - key: UPSTREAM
        value: https://openrouter.ai

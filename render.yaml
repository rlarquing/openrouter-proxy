services:
  - type: web
    name: openrouter-proxy
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    # Sin variables de entorno: el default de UPSTREAM vive en index.js
    # (https://openrouter.ai). Mismo patrón que anthropic-proxy.
